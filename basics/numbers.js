let age = 40;
let dogYears = (age + 1) / 7;

console.log(dogYears);

// challenge

let studentScore = 41;
let maxScore = 100;
let percent = (studentScore / maxScore) * 100;

console.log(
  `The ${age} year old student scored a ${studentScore} out of ${maxScore}.`
);
console.log(`The students average is ${percent} percent`);
